import { Loading } from 'react-daisyui';

function Loader() {
    return (
        <div className='p-20 w-full flex justify-center'>
            <Loading />
        </div>
    );
}

export default Loader;